---
elasticsearch:
  prefix:
    config: /usr/local/etc/elasticsearch
  cluster: {}
  elasticsearch.yml:
    path.plugins: /var/db/elasticsearch/plugins
    path.data: /var/db/elasticsearch/data
    index.number_of_shards: 1
    index.number_of_replicas: 0
